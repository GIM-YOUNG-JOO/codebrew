<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.springframework.dao.mybatis.BoardDao">

	<!-- <insert id="insert" parameterType="Board">
			<selectKey keyProperty="boid" order="BEFORE" resultType="int">
			
			select seq_boid.nextval from dual
			</selectKey>
		
		insert into board(boid, acid, botitle, bocontent, bonewdate, bohitcount, bicd)
		values(#{boId}, #{acId}, #{boTitle}, #{boContent}, sysdate, #{boHitcount}, #{biCd} )
		
	</insert> -->
	
</mapper>