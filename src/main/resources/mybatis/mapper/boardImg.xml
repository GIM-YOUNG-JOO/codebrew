<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.codebrew.dao.BoardImgDao" >
	
	<!-- <insert id="insert" parameterType="BoardImg">
			<selectKey keyProperty="biid" order="BEFORE" resultType="int">
			
			select seq_biid.nextval from dual
			</selectKey>
		
		insert into board(biid,boid,bifile)
		values(#{biId}, #{boId}, #{biFile})
		
	</insert> -->
	


</mapper>